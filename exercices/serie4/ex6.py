#!/usr/bin/python
Me saoule celui là

def swap_min_first(lst: liste) -> None:
    min_value = 0
    var_temporaire = 0
    for i in list:
        if i < min_value: 
            min_value = i
        var_temporaire = lst[0]
        lst[0] = min_value 
